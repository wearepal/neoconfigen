[tool.black]
line-length = 88
target-version = ['py38']
include = '\.pyi?$'
exclude = '''
(
  /(
      .eggs         # exclude a few common directories in the
    | .git          # root of the project
    | .hg
    | .mypy_cache
    | hydra/grammar/gen
    | tools/configen/example/gen
    | .tox
    | .nox
    | website
    | .venv
    | _build
    | dist
  )
)
'''
